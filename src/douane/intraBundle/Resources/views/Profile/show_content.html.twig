{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show form">
    <p style="margin-bottom: 25px; margin-top: 45px;">{{ 'profile.show.username'|trans }}: {{ user.username }}</p>
    <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>
</div>
